#include <bits/stdc++.h>
using namespace std;
int main() {
	int N Q;
	scanf("%d %d", &N, &Q);
	for(int i = 1; i < N; i++) {
		int p, q;
		long long r;
		scanf("%d %d %lld", &p, &q, &r);
	}
	for(int i = 0; i < Q; i++) {
	
		scanf("%d %d", &k, &v);
	}
	return 0;	
}
